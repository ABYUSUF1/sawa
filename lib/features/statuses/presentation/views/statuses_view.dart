import 'package:flutter/material.dart';

class StatusesView extends StatelessWidget {
  const StatusesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Statuses View"));
  }
}
